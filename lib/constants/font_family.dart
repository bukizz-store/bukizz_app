import 'package:flutter/material.dart';


class FontFamily {
  final String name;

  const FontFamily(this.name);

  // You may define more font families as needed
  static const FontFamily defaultFamily = FontFamily('Default');
}